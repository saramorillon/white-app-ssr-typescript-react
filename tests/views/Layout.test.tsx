describe('Layout', () => {
  it('Should render children inside main section', () => {
    // render(<Layout>I&apos;m a child!</Layout>)
    // expect(screen.getByText("I'm a child!")).toBeInTheDocument()
  })
})
