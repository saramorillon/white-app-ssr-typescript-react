# [WHITE APP] SSR Typescript React

## Installation

Replace `white-app-ssr-typescript-react` by your app name in `package.json`

Replace `[WHITE APP] SSR Typescript React` by your app name in `src/views/Header.tsx`

Run `yarn install`
